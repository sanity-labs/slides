# **NAME** Design Guidelines

<!-- Template authors: replace this with your brand's design rules. -->
<!-- This file is loaded as the template's `skill` and surfaced through -->
<!-- the `slides_guidelines` MCP tool so the agent follows your rules. -->

## Component selection

- Use `Cover` as the first slide of every deck.

## Visual rules

- Keep body text at `text-xl` or larger for projection readability.
- Use the template's brand-token colors, never hex literals.
